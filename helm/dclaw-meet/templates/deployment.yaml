apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dclaw-meet.fullname" . }}
  labels:
    {{- include "dclaw-meet.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "dclaw-meet.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "dclaw-meet.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "dclaw-meet.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8106
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
